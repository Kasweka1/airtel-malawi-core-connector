# CircleCI configuration file
# Mojaloop build Orb
version: 2.1

setup: true
orbs:
  build: mojaloop/build@1.0.64
workflows:
  test-build:
    jobs:
      - build/build_local:
          name: Build
          context: org-global

          